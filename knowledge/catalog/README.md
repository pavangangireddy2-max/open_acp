# Knowledge Catalog

This folder is for canonical catalog-style source inputs that sit above runtime wiki state.

Examples:

- base course abstracts
- course alias mappings
- NIAT batch BOS references
- NIAT grid-template references

Rule:

- files here are tracked canonical inputs
- runtime wiki summaries derived from these files belong under `storage/wiki`
